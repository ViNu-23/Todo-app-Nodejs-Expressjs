<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form p-10 text-white">
        <form action="/create" method="post">
          <input
            class="block px-5 w-full py-2 rounded-md bg-zinc-800 outline-none"
            type="text"
            placeholder="Title"
            name="title"
          />
          <textarea
            class="block w-full mt-2 px-5 rounded-md py-2 bg-zinc-800 resize-none outline-none"
            placeholder="More details"
            name="details"
          ></textarea>
          <input
            class="mt-4 bg-blue-600 px-5 py-2 rounded-md hover:bg-blue-400"
            type="submit"
            value="Create"
          />
        </form>
      </div>
      <div class="tasks p-10 flex gap-3 flex-wrap">
        <% if(files.length>0){ %> <% files.forEach(function(val){ %>
        <div class="task w-72 px-3 py-4 bg-zinc-800 rounded-md">
          <h1 class="text-white text-2xl tracking-tighter"><%=val%></h1>
          <div class="mt-3 flex w-full justify-between item-center">
            <a class="text-blue-500 inline-block " href="/files/<%= val %>"
              >Read More.</a
            >
            <a class="text-zinc-600" href="/edit/<%= val %>">edit</a>
          </div>
        </div>
        <% }) %> <% } else { %>
        <h3 class="text-zinc-600">No tasks yet.</h3>
        <% } %>
      </div>
    </div>
  </body>
</html>
